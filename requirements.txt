# Core (always required, no network needed for the default synthetic dataset)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
pyyaml>=6.0
pytest>=7.4

# Optional: enables CIFAR-10 / Fashion-MNIST (torchvision auto-download)
# and GPU-accelerated CNN training via fedpriv.models.torch_models
# torch>=2.1
# torchvision>=0.16

# Optional: experiment tracking
# tensorboard>=2.14
